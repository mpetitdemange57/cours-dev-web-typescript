/**
 * TODO: create a generic interface "BackendResponse" that represent the format of data returned by the backend. It must have properties :
 * - data which is type of the generic provided
 * - metadata which is an object with a property "url" of type string
 */
