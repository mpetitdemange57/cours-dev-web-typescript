/**
 * TODO: create an interface "TaskProperties". this interface must have theses properties :
 * - id, title, type and owner which are all string. owner can also be null.
 * - dueDate is optional and is a type Date.
 */
